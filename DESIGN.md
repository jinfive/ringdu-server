# Ringdu Design

Ringdu는 "Linking Edu"를 의미하는 학원 관리 웹/앱 서비스다. 현재 저장소는 Spring Boot 백엔드이며, 화면 UI는 별도 `ringdu-web` 프로젝트에서 다룬다. 이 문서는 백엔드 관점의 제품 사용 흐름, API 사용성, 권한별 경험 기준을 정리한다.

## 제품 구조

Ringdu의 핵심 사용자는 플랫폼 관리자, 원장, 데스크, 선생님, 학부모, 학생이다. 백엔드는 각 사용자가 자기 권한 범위 안에서 학원, 회원, 반, 시간표, 출석, 숙제, 청구서, 납부 상태를 관리하거나 조회할 수 있도록 API를 제공한다.

초기 화면 구조는 대시보드형 서비스를 전제로 한다.

- `ADMIN`: 전체 학원과 원장 계정을 운영 관점에서 관리한다.
- `OWNER`: 소속 학원의 운영 대시보드에서 구성원, 반, 시간표, 출석, 청구, 공지를 관리한다.
- `DESK`: 학생/학부모/반 배정/출석/납부 같은 운영 보조 업무를 처리한다.
- `TEACHER`: 담당 반, 담당 학생, 출석, 숙제 중심으로 사용한다.
- `PARENT`: 연결된 자녀의 시간표, 출석, 숙제, 청구/납부 상태를 조회한다.
- `STUDENT`: 본인의 시간표, 출석, 숙제와 제출 상태를 조회한다.

## 인증 및 가입 흐름

현재 코드에는 `/api/auth/signup`, `/api/auth/login`, `/api/auth/refresh`, `/api/auth/logout`, `/api/auth/me` 흐름이 존재한다. 일반 회원가입은 `TEACHER`, `PARENT`, `STUDENT`만 허용한다.

- `ADMIN`: 일반 회원가입으로 만들지 않는다.
- `OWNER`: 일반 회원가입으로 만들지 않고, `ADMIN`이 생성하거나 권한을 부여하는 흐름으로 확장한다.
- `DESK`: 일반 회원가입으로 만들지 않고, `OWNER`가 등록 또는 초대하는 흐름으로 확장한다.
- `TEACHER`, `PARENT`, `STUDENT`: 이메일/비밀번호 기반 일반 회원가입을 허용한다.

Access Token은 응답 body로 반환하고, Refresh Token은 `HttpOnly` Cookie로 내려주는 정책을 기준으로 한다. 프론트엔드는 Access Token을 JavaScript 메모리 변수에 저장하는 전제를 둔다.

## API 사용성 기준

모든 API는 가능한 한 `ApiResponse` 형식을 따른다. Entity를 직접 반환하지 않고 요청 DTO와 응답 DTO를 분리한다. URL은 `/api`로 시작하고 복수형 리소스명을 우선한다.

권한 검증은 사용자 경험의 일부다. 사용자가 볼 수 없는 학원, 학생, 출석, 숙제, 청구 정보를 응답하지 않아야 하며, 잘못된 접근은 일관된 오류 응답으로 처리한다.

## 도메인 설계 방향

`User`는 로그인 계정 정보만 관리한다. 학원명, 학원주소, 학원코드, 학원전화번호는 `User`에 넣지 않는다.

예정 도메인 분리는 아래 기준을 따른다.

- `Academy`: 학원 기본 정보
- `AcademyMember`: 학원과 `OWNER`, `DESK`, `TEACHER` 계정의 관계
- `Student`: 학생 프로필
- `StudentGuardian`: 학부모와 학생의 관계
- `AcademyStudent`: 학원과 학생의 관계

현재 코드에는 `User`, `RefreshToken`, 인증/보안 공통 구조가 있고, 학원/학생/반/출석/숙제/청구 도메인은 예정 상태다.
